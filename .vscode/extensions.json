{
  // install the clangd extension
  "recommendations": ["llvm-vs-code-extensions.vscode-clangd"],
  // we don't want microsoft's c/c++ extension
  // we use `just` to handle cmake configuration, so cmake-tools would be confusing
  "unwantedRecommendations": ["ms-vscode.cpptools", "ms-vscode.cmake-tools"]
}
